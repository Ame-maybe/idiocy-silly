package main

import "fmt"

// There are many components in the catalog of cmd in a golang project.
// we must distinguish each component's duty such as doc、auth etc.
func main() {
	fmt.Println("Hello idiocy silly....")
}
